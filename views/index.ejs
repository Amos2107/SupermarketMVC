<%- include('partials/header') %>
<%- include('partials/navbar') %>

<div class="container my-5">
  <div class="hero mb-5 fade-up">
    <div class="row align-items-center">
      <div class="col-lg-7">
        <div class="d-flex flex-wrap align-items-center mb-3 gap-2">
          <span class="pill">Fast checkout</span>
          <span class="pill">Fresh picks daily</span>
          <span class="pill">Secure payments</span>
        </div>
        <h1 class="display-5 fw-bold">Welcome to AmosMart</h1>
        <p class="lead mb-4">Shop smarter, manage inventory faster, and keep every basket moving.</p>

        <% if (user) { %>
          <% if (user.role === 'admin') { %>
            <div class="d-flex flex-wrap gap-2">
              <a href="/inventory" class="btn btn-light text-primary fw-semibold px-4">Go to Inventory</a>
              <a href="/admin/inventory-health" class="btn btn-ghost fw-semibold px-4">Inventory Health</a>
            </div>
          <% } else { %>
            <div class="d-flex flex-wrap gap-2">
              <a href="/shopping" class="btn btn-light text-primary fw-semibold px-4">Start Shopping</a>
              <a href="/cart" class="btn btn-ghost fw-semibold px-4">View Cart</a>
            </div>
          <% } %>
        <% } else { %>
          <div class="d-flex flex-wrap gap-2">
            <a href="/login" class="btn btn-light text-primary fw-semibold px-4">Login</a>
            <a href="/register" class="btn btn-ghost fw-semibold px-4">Register</a>
          </div>
        <% } %>
      </div>
      <div class="col-lg-5 mt-4 mt-lg-0">
        <div class="glass p-4 text-dark h-100">
          <h5 class="fw-semibold mb-3 text-dark">Why AmosMart?</h5>
          <ul class="list-unstyled mb-0 text-dark">
            <li class="mb-2 d-flex align-items-center">
              <span class="badge bg-primary me-2">1</span>
              Fresh picks with live stock so you never hit an out-of-stock.
            </li>
            <li class="mb-2 d-flex align-items-center">
              <span class="badge bg-primary me-2">2</span>
              Smooth cart and checkout with instant updates as you shop.
            </li>
            <li class="d-flex align-items-center">
              <span class="badge bg-primary me-2">3</span>
              Save your details and reorder favorites in a couple of taps.
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <div class="row g-3">
    <div class="col-md-4">
      <div class="card h-100 border-0 benefit-card card-hover">
        <div class="card-body d-flex gap-3">
          <div class="benefit-icon">⚡</div>
          <div>
            <h5 class="fw-semibold">Fast shopping</h5>
            <p class="text-muted mb-0">Browse, add to cart, and check out quickly—your picks stay saved while you browse.</p>
          </div>
        </div>
      </div>
    </div>
    <div class="col-md-4">
      <div class="card h-100 border-0 benefit-card card-hover">
        <div class="card-body d-flex gap-3">
          <div class="benefit-icon">📦</div>
          <div>
            <h5 class="fw-semibold">Smart inventory</h5>
            <p class="text-muted mb-0">See what’s in stock before you buy and get suggestions for the best picks.</p>
          </div>
        </div>
      </div>
    </div>
    <div class="col-md-4">
      <div class="card h-100 border-0 benefit-card card-hover">
        <div class="card-body d-flex gap-3">
          <div class="benefit-icon">🔒</div>
          <div>
            <h5 class="fw-semibold">Secure sessions</h5>
            <p class="text-muted mb-0">Safe checkout with clear confirmations so you always know what’s happening.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
