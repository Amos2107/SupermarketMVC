<nav class="navbar navbar-expand-lg navbar-dark">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">AmosMart</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#amosNavbar">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="amosNavbar">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <% if (currentUser) { %>

          <li class="nav-item">
            <a class="nav-link <%= currentPath === '/shopping' ? 'active' : '' %>" href="/shopping">Shop</a>
          </li>

          <% if (currentUser.role === 'admin') { %>

            <li class="nav-item">
              <a class="nav-link <%= currentPath.startsWith('/inventory') ? 'active' : '' %>" href="/inventory">Inventory</a>
            </li>

            <li class="nav-item">
              <a class="nav-link <%= currentPath === '/addProduct' ? 'active' : '' %>" href="/addProduct">Add Product</a>
            </li>

            <li class="nav-item">
              <a class="nav-link <%= currentPath.startsWith('/admin/orders') ? 'active' : '' %>" href="/admin/orders">All Orders</a>
            </li>

            <li class="nav-item">
              <a class="nav-link <%= currentPath.startsWith('/admin/users') ? 'active' : '' %>" href="/admin/users">Users</a>
            </li>

          <% } else { %>

            <li class="nav-item">
              <a class="nav-link <%= currentPath.startsWith('/orders') ? 'active' : '' %>" href="/orders">My Orders</a>
            </li>

          <% } %>

        <% } else { %>

          <li class="nav-item">
            <a class="nav-link <%= currentPath === '/' ? 'active' : '' %>" href="/">Home</a>
          </li>

        <% } %>

      </ul>

      <ul class="navbar-nav align-items-lg-center gap-2">
        <% if (!currentUser) { %>

          <li class="nav-item">
            <a class="nav-link <%= currentPath === '/register' ? 'active' : '' %>" href="/register">Register</a>
          </li>

          <li class="nav-item">
            <a class="nav-link <%= currentPath === '/login' ? 'active' : '' %>" href="/login">Login</a>
          </li>

        <% } else { %>

          <li class="nav-item">
            <a class="cart-pill text-decoration-none" href="/cart" aria-label="Go to cart">
              <span aria-hidden="true">🛒</span>
              <span class="fw-semibold">Cart</span>
            </a>
          </li>

          <li class="nav-item d-flex align-items-center">
            <span class="navbar-text me-2 text-white-75">Hi,</span>
            <div class="d-flex align-items-center gap-2">
              <div class="d-flex flex-column lh-sm">
                <strong class="text-white"><%= currentUser.username %></strong>
                <small class="text-white-50 text-uppercase"><%= currentUser.role %></small>
              </div>
            </div>
          </li>

          <li class="nav-item">
            <a class="nav-link" href="/logout">Logout</a>
          </li>

        <% } %>
      </ul>
    </div>
  </div>
</nav>
